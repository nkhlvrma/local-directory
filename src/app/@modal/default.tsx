// Parallel route slot for intercepted "sheet on top of the current page"
// routes (see @modal/(.)list-your-business). Renders nothing when no
// intercepted route is active — i.e. on every normal page.
export default function Default() {
  return null;
}
